<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndruschenkoApps"
        tools:targetApi="31">
        <activity
            android:name=".apps.customview.CustomViewCombineActivity"
            android:exported="false" />
        <activity
            android:name=".apps.handlerlooper.HandlerLooperActivity2"
            android:exported="false" />
        <activity
            android:name=".apps.handlerlooper.HandlerLooperActivity"
            android:exported="false"
            android:theme="@style/Theme.HandlerLooper.Base" />
        <activity
            android:name=".apps.recyclerview.RecyclerViewActivity"
            android:exported="false" />
        <activity
            android:name=".apps.sharedpreferences.SharedPreferencesActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.AndruschenkoApps">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
